const myArr = [3,1,4,2, 0, -2, 18, -12, 5, 6]
//need to decide how many buckets to use. it's not clear, so I can do sqrt(myArr.length), which = 3

function sortArray(arr) {
    
}

console.log(sortArray(myArr))